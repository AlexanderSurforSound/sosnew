'use client';import Link from 'next/link';import { cn } from '@/lib/utils';import { PriceDisplay } from './PriceDisplay';interface SpecialRateCardProps {  checkIn: Date;  checkOut: Date;  originalWeeklyRate: number;  specialWeeklyRate: number;  discountPercent: number;  checkoutUrl: string;  className?: string;}export function SpecialRateCard({  checkIn,  checkOut,  originalWeeklyRate,  specialWeeklyRate,  discountPercent,  checkoutUrl,  className,}: SpecialRateCardProps) {  const formatDate = (date: Date) => {    return new Intl.DateTimeFormat('en-US', {      month: 'short',      day: 'numeric',    }).format(date);  };  return (    <div      className={cn(        'flex flex-col items-center justify-center px-4 py-6 bg-white rounded-lg',        className      )}    >      {/* Date Range */}      <div className="flex items-center justify-center gap-3 mb-4">        <span className="text-primary font-bold text-sm border-b-2 border-info">          {formatDate(checkIn)}        </span>        <span className="text-primary" aria-hidden="true">to</span>        <span className="text-primary font-bold text-sm border-b-2 border-info">          {formatDate(checkOut)}        </span>      </div>      {/* Price Calculator Button */}      <div className="flex flex-col md:flex-row items-center justify-center gap-4">        <button className="relative p-4 border-2 border-success rounded-lg bg-white hover:bg-success/5 transition-colors">          {/* Special Badge */}          <span className="absolute -top-3 left-1/2 -translate-x-1/2 bg-success text-white text-xs font-bold px-3 py-1 rounded-full whitespace-nowrap">            On Special          </span>          {/* Pricing */}          <div className="flex items-center gap-2 text-primary">            <span className="line-through opacity-70">              ${originalWeeklyRate.toLocaleString()}            </span>            <span className="font-bold text-success">{discountPercent}% off</span>          </div>          {/* Special Rate */}          <div className="text-3xl font-bold text-primary mt-1">            ${specialWeeklyRate.toLocaleString()}            <span className="text-sm font-normal opacity-70">/week</span>          </div>        </button>        {/* Book Now Button */}        <Link          href={checkoutUrl}          className="bg-secondary-700 hover:bg-secondary-800 text-white font-bold py-3 px-6 rounded-lg transition-colors whitespace-nowrap focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-secondary-700"        >          Book Now        </Link>      </div>    </div>  );}